## FUNCTION CALL
Инструкции к вызову функций.

### Взаимодействие с окружением
Воздействие на внешний мир осуществляется через универсальный маршрутизатор `execute_skill`.
Требования к полезной нагрузке:
1. `thoughts`: Мысли. Логика принятия решений.
2. `actions`: Массив инструментов для исполнения. Можно вызывать параллельно. Если функция требует передачи аргументов (в описании есть флаг <REQUERED>) - необходимо передавать их в блоке parameters сооветствующей функции.


### Завершение итерации
Для штатного завершения текущего вычислительного цикла необходимо передать пустой массив: `"actions"=[]`.


### Пример вызова инструментов

```json
{
  "thoughts": "...мысли...",
  "actions": [
    {
      "tool_name": "HostOSNetwork.ping_host",
      "parameters": {
        "host": "192.168.1.10",
        "count": 4
      }
    },
    {
      "tool_name": "TelethonMessages.send_message",
      "parameters": {
        "to_id": 123456789,
        "text": "...текст..."
      }
    }
  ]
}
```